//  通常cpu分为32位和64位

8位等于一个字节

即：（理论）
32位cpu一次运算可以处理4个字节的数据

64位cpu一次运算可以处理8个字节的数据

如果不大的数据使用bigint，(8个字节)

则：

64位cpu需要运算1次，读出数据，

而32位cpu则需要运算2次，才能读出数据，

这样势必会影响数据的读取和处理

虽说CPU每秒能运算20亿次左右，但访问量大的话，必然会影响性能和响应速度，而且cpu需要处理的数据很多，如果一个数据需要运算2次，结果可想而知
